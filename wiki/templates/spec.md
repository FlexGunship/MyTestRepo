# Spec NNN-XX — <title>

## Status
- Doc type: <implementation | investigation | bugfix>
- Executes: <CC | GD | CX>; merge: <self-merge (graduated) | integrator-merged (onboarding) | author==executor flagged>
- Number NNN verified free (highest in wiki/specs/ = MMM; NNN = MMM + 1).
- Paired prompt: prompt-specNNN-XX-slug.md
- Final on-disk locations after merge: <paths>

## Background
<why this work exists; institutional context; the problem it solves>

## Decisions made
1. <Manager call>
2. <Manager call>

The developer implements as stated unless they find a strong reason to deviate — in which case
they flag it in the report rather than silently choosing otherwise.

## Scope — what to build
<subsections as needed: directory structure, components, data shapes, behavior, tooling>

## Out of scope
- <explicit exclusion>

## Working model
(Detail in the prompt file — prompt-specNNN-XX-slug.md.)

## Definition of done
- [ ] <criterion>
- [ ] Gate green. Merged.

## Deliverable / report-back
See the prompt file for the report-back format.
