# MyTestRepo

Multi-agent development sandbox. Three worktrees under `worktrees/`:

- **CM** — Claude Manager (`agent/cm`)
- **CC** — Claude Code (`agent/cc`)
- **CX** — Codex (`agent/cx`)

The primary clone lives in `repo-master`.
